# Blocca l'accesso diretto via browser a tutti i file di storage CSV
# (contengono, tra l'altro, gli hash delle password). Apache 2.4+ e 2.2.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
